drop the "visualconfig" folder in tf/cfg;
 if you installed the other half of the config the cfg inside should automatically execute; if not, type:

 " execute visualconfig/visualconfig.cfg " at the end of your autoexec.cfg

There is a huge selection of great huds in the huds folder along with screenshots;
 simply drop the desired huds folder in custom and it should do the trick.